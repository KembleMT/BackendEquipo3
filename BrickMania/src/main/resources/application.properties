# Nombre de la aplicación
spring.application.name=BrickMania

# Configuración de la base de datos MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/brickmania?serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Pa$$w0rd

# Dialecto de MySQL 8 para Hibernate
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Actualiza automáticamente las tablas si hay cambios en los modelos
spring.jpa.hibernate.ddl-auto=update

# Muestra las consultas SQL en la consola (Opcional, pero útil para depuración)
spring.jpa.show-sql=true
